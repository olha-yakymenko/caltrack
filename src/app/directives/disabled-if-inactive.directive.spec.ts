import { DisabledIfInactiveDirective } from './disabled-if-inactive.directive';

describe('DisabledIfInactiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledIfInactiveDirective();
    expect(directive).toBeTruthy();
  });
});
